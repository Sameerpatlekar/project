apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mysql
  namespace: backend
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data
